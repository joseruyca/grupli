# V14.2 — Compile fix

Corrige errores de compilación de V14.1:

- Uso de `AppData.asList` y `AppData.asMap` dentro del dashboard del grupo.
- Apertura de `CreateExpenseScreen` con `groupId`, que es el parámetro real.
- Limpieza de regla de lint no reconocida en `analysis_options.yaml`.

No hay SQL nuevo obligatorio.
