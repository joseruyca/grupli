# Grupli v15.18.1 — Compile fix

Corrige un error de sintaxis en `eventRoutineBadge`.

## Cambio
- Se arregla la expresión regular que detecta rutinas antiguas en las notas del evento.
- Se elimina un import no usado de `dart:ui`.

## SQL
No requiere SQL nuevo.
