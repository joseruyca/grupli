# v12.7 Compile fix

- `money(share.toDouble())` para evitar `num` vs `double`.
- `EmptySlim.body` pasa a ser opcional.
- Se elimina `deprecated_member_use` de `analysis_options.yaml` porque no es una regla linter válida.
